# C8 — LCP 가 이미지일 때

> 사전 등록 → [PREDICTION.md](PREDICTION.md) · 측정 [measure.mjs](measure.mjs) · 집계 [analyze.mjs](analyze.mjs)
> 측정 총 **206회** — 본 측정 72 · 반증 60 · eager 실제판 60 · 순서 14. 실패 0건. 2026-09-18 ~ 19.

## 한 줄

**예측이 틀렸다.** `content-visibility` 는 이미지 LCP 도 12~38% 앞당겼다.
그런데 **다운로드가 빨라진 게 아니라 요청이 일찍 나갔다** — 그리고 그건 LCP 이미지가 `loading="lazy"` 였기 때문이다.

`eager` 가 실제인 사이트(Wikivoyage 3곳)에서 다시 재보니 **기전은 확인됐지만 이득은 없었다.**
요청이 첫 레이아웃 **전에** 나가고(14/14), 레이아웃 예산도 30~70ms 뿐이라 앞당길 게 없다.

> `content-visibility` 가 이미지 LCP 를 앞당기려면 **둘 다** 필요하다 —
> ① LCP 이미지가 `lazy` ② 레이아웃 비용이 수백 ms.

---

## 0. 대상을 찾는 데 세 번 걸렸다

[C7](../c7-real-initial-load/RESULTS.md) 의 3곳은 전부 문서 사이트라 **FCP = LCP** 였고,
그래서 사전 등록한 R3(FCP/LCP 분리)을 측정조차 못 했다. 이미지 LCP 페이지가 필요했다.

| 시도 | 결과 |
|---|---|
| 위키 본문 4곳 (데스크톱 1280px) | 전부 텍스트 LCP. 썸네일이 작아 문단이 더 크다 |
| 갤러리·포토 4곳 | 이미지 LCP 는 나오는데 문서가 1,519~3,076px — cv 가 의미 없다 |
| **모바일 폭 390×844** | **뒤집혔다.** 문단 면적이 줄고 이미지가 전체 폭을 차지한다 |

| 사이트 | 문서 높이 | DOM | 이미지 | LCP | 블록 | 중앙값 | 변동계수 |
|---|---:|---:|---:|---|---:|---:|---:|
| Grand Canyon | 48,963px | 7,383 | 69 | `<img>` 53,280px² | 224 | 208px | 2.83 |
| Milky Way | 71,931px | 11,237 | 46 | `<img>` 52,080px² | 225 | 234px | 4.56 |
| Mount Everest | 91,810px | 15,289 | 233 | `<img>` 55,296px² | 429 | 200px | 3.83 |

---

## 1. 연기 테스트가 설계 결함을 잡았다

[C7 의 "캐시를 데운 뒤 잰다"](../c7-real-initial-load/PREDICTION.md) 를 그대로 가져왔더니 **FCP = LCP 가 또 나왔다.**
LCP 요소는 제대로 `<img>` 인데도.

```
cv-auto   FCP 684.3  LCP 684.3  이미지<img>  Layout  52.28
plain     FCP 595.3  LCP 595.3  이미지<img>  Layout 198.42
```

캐시가 따뜻하면 **이미지도 캐시돼 있어서** 첫 컨텐츠 프레임에 같이 그려진다.

> 문서 사이트에서 네트워크는 **잡음**이었다. 이미지 LCP 에서는 **현상 그 자체**다.
> 다운로드를 지워버리면 재려던 걸 지우는 것이다.

데이터 수집 **전에** 캐시 상태를 축으로 올렸다 — `warm`(C7 동일) / `cold`(빈 프로필 첫 로드).
[PREDICTION.md 부칙](PREDICTION.md#부칙--측정-전-설계-변경-2026-09-18-본-측정-시작-전) 에 그 경위를 남겼다.

---

## 2. 렌더링 일 — 예상대로 크게 준다

로드 중 총 ms · 중앙값 · 6회

| 사이트 | 캐시 | 모드 | Style | Layout | Paint | Layout 절감 |
|---|---|---|---:|---:|---:|---:|
| Milky Way | cold | plain | 328.7 | 618.1 | 26.8 | |
| | | cv-auto | 64.5 | **216.6** | 17.8 | **−65%** |
| | warm | plain | 299.1 | 262.5 | 22.6 | |
| | | cv-auto | 93.4 | **27.2** | 16.3 | **−90%** |
| Grand Canyon | cold | plain | 267.7 | 478.3 | 25.8 | |
| | | cv-auto | 69.0 | **124.7** | 22.7 | **−74%** |
| | warm | plain | 290.6 | 229.3 | 25.8 | |
| | | cv-auto | 73.0 | **41.1** | 13.7 | **−82%** |
| Mount Everest | cold | plain | 379.1 | 643.5 | 41.9 | |
| | | cv-auto | 124.2 | **311.7** | 57.3 | **−52%** |
| | warm | plain | 485.7 | 405.5 | 67.3 | |
| | | cv-auto | 102.8 | **67.9** | 32.0 | **−83%** |

**S1 성립.** 여섯 조건 전부 50% 이상 줄었다.
`cold` 절감폭(52~74%)이 `warm`(82~90%)보다 작은 건, 콜드에서는 이미지가 하나씩 도착할 때마다
레이아웃이 다시 돌기 때문이다 — cv 가 건너뛴 부분이 아니라 화면 안쪽에서 생기는 일이다.

문서 높이는 `cv-auto` 에서 55~70% 로 줄었다 (`contain-intrinsic-size` 가 실제보다 작게 잡은 것 — [C6 와 같은 성질](../c6-real-sites/RESULTS.md)).

---

## 3. FCP 와 LCP 가 드디어 갈라졌다

ms · 중앙값

| 사이트 | 캐시 | 모드 | FCP | LCP | LCP−FCP | FCP 변화 | LCP 변화 |
|---|---|---|---:|---:|---:|---:|---:|
| Milky Way | cold | plain | 2183 | 2349 | **167** | | |
| | | cv-auto | 1642 | 1935 | **293** | −25% | −18% |
| | warm | plain | 631 | 631 | 0 | | |
| | | cv-auto | 604 | 629 | 25 | −4% | −0% |
| Grand Canyon | cold | plain | 1893 | 2224 | **331** | | |
| | | cv-auto | 1305 | 1597 | **292** | −31% | −28% |
| | warm | plain | 905 | 905 | 0 | | |
| | | cv-auto | 561 | 561 | 0 | −38% | −38% |
| Mount Everest | cold | plain | 2230 | 2346 | **117** | | |
| | | cv-auto | 1810 | 2064 | **254** | −19% | −12% |
| | warm | plain | 901 | 1033 | 132 | | |
| | | cv-auto | 655 | 655 | 0 | −27% | −37% |

**S5 성립.** `cold` 에서 LCP−FCP 가 117~331ms 로 벌어졌다.
[C7 에서 못 쟀던 것](../c7-real-initial-load/RESULTS.md)을 처음 쟀다.

**S7 은 부분 성립.** `warm` 에서 여섯 중 넷은 FCP = LCP 였지만 Everest plain 은 132ms 갈렸다.

### 그리고 S2·S6 이 반증됐다

**"이미지 LCP 는 다운로드에 묶여 있으니 cv 로 안 빨라진다"** 고 예측했다. 틀렸다.
`cold` 에서 12~28%, `warm` 에서 0~38% 빨라졌다.

---

## 4. 왜 빨라졌나 — 다운로드가 빨라진 게 아니다

LCP 이미지의 리소스 타이밍을 직접 봤다. ms · 중앙값

| 사이트 | 캐시 | 모드 | 요청 시작 | 응답 끝 | 전송 시간 | 바이트 | LCP |
|---|---|---|---:|---:|---:|---:|---:|
| Grand Canyon | cold | plain | 1835 | 2111 | 276 | 38,064 | 2224 |
| | | cv-auto | **1256** | 1376 | 120 | 38,064 | **1597** |
| Milky Way | cold | plain | 2094 | 2221 | 127 | 19,938 | 2349 |
| | | cv-auto | **1560** | 1674 | 114 | 19,938 | **1935** |
| Mount Everest | cold | plain | 2114 | 2226 | 112 | 28,194 | 2346 |
| | | cv-auto | **1699** | 1827 | 127 | 28,194 | **2064** |

**바이트가 같다. 전송 시간도 사실상 같다.** 달라진 건 **요청 시작 시각**뿐이다 — 400~580ms 앞당겨졌다.

`content-visibility` 는 네트워크를 건드리지 않는다. 그런데 요청이 일찍 나갔다.

### 이미지 요청이 왜 1.8초나 뒤에 나가나

프리로드 스캐너가 있으면 훨씬 일찍 나갔어야 한다. [probe-why.mjs](probe-why.mjs) 로 봤다.

| 관측 | plain | cv-auto |
|---|---|---|
| LCP 이미지의 `loading` | **`lazy`** (세 곳 전부) | **`lazy`** |
| 요청된 이미지 수 / 전체 (lazy) | 10/69(63) · 6/46(43) · 18/233(226) | **완전히 동일** |
| 요청된 이미지 바이트 | 110KB · 81KB · 87KB | **완전히 동일** |
| LCP 이미지 위치 | top 369·340·465px (뷰포트 844px 안) | 동일 |

**"cv 가 화면 밖 이미지 요청을 줄여 경쟁이 완화된다" 가설은 틀렸다.** 요청 수도 바이트도 똑같다.

남는 설명은 하나다:

> `loading="lazy"` 이미지는 **"뷰포트 안인가"** 를 알아야 요청이 나간다.
> 그건 **레이아웃을 해야 안다.** 레이아웃이 요청의 선행 조건이다.
> cv 가 레이아웃을 52~74% 줄이면 그 판정이 앞당겨지고, 요청이 먼저 나가고, LCP 가 빨라진다.

이게 맞다면 **`content-visibility` 는 이미지 LCP 를 직접 빠르게 하지 않는다.**
lazy 이미지의 **요청 발사 시점을 앞당겨** 간접적으로 빠르게 하는 것이다.

이 설명은 추측으로 두면 안 된다. → [5. 반증 테스트](#5-반증-테스트--lazy-를-끄면-효과가-사라지는가)

---

## 5. 반증 테스트 — lazy 를 끄면 효과가 사라지는가

> 측정 [measure-lazy.mjs](measure-lazy.mjs) · 집계 [analyze-lazy.mjs](analyze-lazy.mjs) · 60회 (3곳 × 2모드 × lazy/eager × 5회), 콜드 캐시, 실패 0건

HTML 을 가로챌 때 `loading="lazy"` 를 `loading="eager"` 로 전부 바꿨다 (`lazy남음 0/69`·`0/46`·`0/233` 으로 확인).
가설이 맞으면 **eager 에서 cv 의 LCP 개선이 사라져야 한다.**

### LCP 로 보면 2 대 1 로 갈린다

| 사이트 | lazy | eager | |
|---|---:|---:|---|
| Grand Canyon | −13% | **+5%** | 개선이 사라졌다 |
| Mount Everest | −23% | **−9%** | 개선이 사라졌다 |
| Milky Way | −5% | −21% | 반대로 나왔다 |

Milky Way 는 **lazy 팔에서부터 개선이 안 나왔다(−5%).** 그 사이트에서는 대조 자체가 성립하지 않았다.
이 실행의 LCP 흩어짐은 1.3~1.6배이고 효과 크기가 그와 비슷하다 — **LCP 만으로는 판정이 안 된다.**

### 요청 시작으로 보면 3 대 0 이다

LCP 가 아니라 **가설이 직접 예측하는 양**(요청 시작 시각)을 보면 깨끗하다.

| 실행 | 사이트 | 이미지 | Layout 절감 | 요청 앞당김 |
|---|---|---|---:|---:|
| C8 본측정 | Grand Canyon | lazy | 354ms | **+579ms** |
| C8 본측정 | Milky Way | lazy | 402ms | **+534ms** |
| C8 본측정 | Mount Everest | lazy | 332ms | **+414ms** |
| C8b 반증 | Grand Canyon | lazy | 57ms | **+149ms** |
| C8b 반증 | Milky Way | lazy | 104ms | **+73ms** |
| C8b 반증 | Mount Everest | lazy | 98ms | **+59ms** |
| C8b 반증 | Grand Canyon | **eager** | 65ms | **−106ms** |
| C8b 반증 | Milky Way | **eager** | 134ms | **+47ms** |
| C8b 반증 | Mount Everest | **eager** | 118ms | **−98ms** |

- **lazy 6점은 전부 양수.** Layout 을 많이 아낄수록 요청이 많이 앞당겨진다 (r = 0.95)
- **eager 3점은 평균 −52ms.** Layout 절감은 lazy 쪽(57~104ms)과 비슷한데도 요청이 안 앞당겨진다

> **같은 레이아웃 절감이어도, 요청이 앞당겨지는 건 이미지가 `lazy` 일 때뿐이다.**

r = 0.95 는 6점이 두 덩어리(본측정 / 반증실행)로 갈려 있어 그 사이 간격이 만든 값이다.
덩어리 안에서는 그만큼 깨끗하지 않다. 방향과 자릿수만 읽어야 한다.
**진짜 증거는 상관이 아니라 lazy/eager 대조**다 — 같은 실행, 섞인 순서, 부호가 뒤집힌다.

### 곁가지 — eager 는 공짜가 아니다

`eager` 는 요청을 확실히 앞당긴다(plain 기준 1026→728, 1233→856, 1271→884ms).
그런데 LCP 는 그만큼 안 줄었다(−8%, −2%, −23%). 전송이 길어졌기 때문이다:

| | 요청 시작 | 응답 끝 | 전송 |
|---|---:|---:|---:|
| Grand Canyon plain lazy | 1026 | 1149 | 123ms |
| Grand Canyon plain eager | 728 | 1031 | **303ms** |

lazy 를 끄면 화면 밖 이미지 60여 개가 한꺼번에 경쟁한다. 먼저 나가도 늦게 끝난다.

### Layout 절감은 이미지 정책과 무관했다

| 사이트 | lazy | eager |
|---|---:|---:|
| Milky Way | −66% | −72% |
| Grand Canyon | −59% | −61% |
| Mount Everest | −61% | −64% |

**S10 성립.** `content-visibility` 가 아끼는 레이아웃 일 자체는 이미지 로딩 정책과 상관없다.
달라지는 건 **그 절감이 LCP 로 이어지는 경로**다.

### 판정

| # | 주장 | 판정 |
|---|---|---|
| S8 | eager 에서 cv 의 LCP 개선이 사라진다 | **부분 성립** — LCP 로는 2/3, 요청 시작으로는 3/3 |
| S9 | eager 가 plain 의 LCP 를 크게 앞당긴다 | **부분 성립** — 요청은 300~400ms 앞당기나 전송이 길어져 LCP 는 2~23% |
| S10 | eager 에서도 Layout 절감은 그대로 | **성립** — 59~66% vs 61~72% |

### 두 실행을 가로질러 비교하면 안 된다

같은 `cold·plain` 조건인데 Layout 이 본측정 478~643ms, 반증실행 97~161ms 로 **4배 차이**가 났다.
LCP 도 2.2~2.3초에서 1.2~1.8초로 내려갔다. 그 사이 네트워크가 달라졌다.

로드 중 Layout 은 고정 비용이 아니다 — **리소스가 도착할 때마다 다시 도는 양**이라
네트워크가 빠르면 그 횟수 자체가 줄어든다. 위 표에서 실행을 나눠 적은 이유다.

---

## 6. C8c — `eager` 가 '실제'인 사이트에서 다시

> 사전 등록 [부칙 3](PREDICTION.md#부칙-3--c8c-eager-가-실제인-사이트-2026-09-19-측정-전) · 측정 [measure-eager.mjs](measure-eager.mjs) · [probe-order.mjs](probe-order.mjs) · 60회 + 14회

[C8b 의 `eager` 는 내가 만든 조건이었다.](#5-반증-테스트--lazy-를-끄면-효과가-사라지는가)
모든 이미지를 한꺼번에 eager 로 만들어 전송이 123 → 303ms 로 길어지는 부작용이 있었다.

### 대상 — 18곳 중 4곳, 전부 Wikivoyage

| 시도 | 결과 |
|---|---|
| 뉴스·문서·매거진 10곳 | 8곳 탈락 — 텍스트 LCP 이거나 문서가 2~14k px |
| 위키 밖 (seriouseats · dpreview · nps) | **봇 차단** — 문서 744~1,837px, DOM 33~280 |
| Wikivoyage 5곳 | **4곳 통과.** Tokyo 만 LCP 이미지가 lazy |

**Wikivoyage 는 Wikipedia 와 같은 MediaWiki 이고 선택자도 같은데**(`.mw-parser-output section > *`)
히어로 배너에만 `lazy` 가 안 붙는다. 나머지는 대부분 lazy 다 — **요즘 권장되는 패턴 그대로**다.
같은 엔진에서 Tokyo 만 다른 것도 기록해 둔다. 엔진이 아니라 **페이지별**이다.

| 사이트 | 문서 높이 | LCP 이미지 | 블록 | 이미지(lazy) |
|---|---:|---|---:|---|
| Paris | 82,189px | `loading` 없음 | 461 | 246 (179) |
| Rome | 66,411px | `loading` 없음 | 414 | 121 (86) |
| London | 124,619px | `loading` 없음 | 746 | 689 (658) |

[C8 과 세션이 다르면 가로질러 비교할 수 없으므로](#두-실행을-가로질러-비교하면-안-된다) 대조군을 **같은 실행 안에** 뒀다 —
`asis`(손 안 댐) / `forced`(`loading` 속성이 없는 `<img>` 에만 lazy 를 붙임).

> `forced` 는 설계할 때 246개 중 67개가 바뀔 줄 알았는데 **실제로는 3개**만 바뀌었다.
> 나머지는 JS 가 만드는 이미지라 HTML 치환이 닿지 않는다.
> 결과적으로 조작이 **히어로에 거의 국한**됐다 — C8b 보다 깨끗한 조건이 됐다.

### LCP 로는 판정이 안 된다

| 사이트 | 팔 | `plain` | `cv-auto` | LCP 변화 |
|---|---|---:|---:|---:|
| Paris | `asis` | 1470 | 1229 | −16% |
| | `forced` | 1398 | 1396 | −0% |
| London | `asis` | 1546 | 1579 | +2% |
| | `forced` | 1479 | 1430 | −3% |
| Rome | `asis` | 1596 | 1421 | −11% |
| | `forced` | 1490 | 1413 | −5% |

예측(T1: `asis` 에서 효과 없음 / T2: `forced` 에서 효과 있음)과 **정확히 반대 방향**으로 보인다.
그런데 이건 결과가 아니라 **검정력이 없다는 뜻**이다.

| | |
|---|---|
| 기전이 쓸 수 있는 예산 (Layout 절감) | **30~70ms** |
| 요청 시작의 셀 내 MAD 중앙값 | **83ms** → 추정 표준편차 122ms |
| 검정력 0.8 에 필요한 반복 | **셀당 약 96회** (실제로는 5회) |

[C8 본측정의 예산은 330~400ms](#4-왜-빨라졌나--다운로드가-빨라진-게-아니다) 였다 — **6~8배 유리한 조건**이었다.
이 페이지들은 `plain` Layout 이 104~135ms 밖에 안 된다. 아낄 게 없으면 앞당길 것도 없다.

**설계를 짤 때 이 계산을 먼저 했어야 했다.** 60회를 돌리고 나서 알았다.

### 순서로 보면 14/14, 예외 없다

기전이 예측하는 건 **시간 차이가 아니라 순서**다:

> LCP 이미지가 **eager** → 요청이 **첫 레이아웃 전에** 나간다 (파싱 시점)
> LCP 이미지가 **lazy** → 요청이 **첫 레이아웃 뒤에** 나간다 (뷰포트 안인지 알아야 하니까)

이건 네트워크가 빠르든 느리든 부호가 안 바뀐다. 30ms 를 잡을 필요가 없다.

| 사이트 | 팔 | LCP 이미지 | 첫 Layout 끝 | 요청 시작 | **요청 전 끝난 Layout** |
|---|---|---|---:|---:|---:|
| Paris | `asis` | `none` | 893 · 1277 | 758 · 1047 | **0** / 135 · 137 |
| | `forced` | `lazy` | 930 · 872 | 1015 · 959 | **4** / 135 |
| Rome | `asis` | `none` | 849 · 955 | 723 · 808 | **0** / 72 · 71 |
| | `forced` | `lazy` | 866 · 715 | 957 · 812 | **4** / 68 |
| London | `asis` | `none` | 1099 · 1444 | 933 · 1331 | **0** / 81 · 78 |
| | `forced` | `lazy` | 1044 · 964 | 1130 · 1044 | **5** / 80 · 81 |
| **Grand Canyon**<br><sub>양성 대조 · 실제 lazy</sub> | `asis` | `lazy` | 850 · 944 | 998 · 1149 | **6 · 8** / 54 |

**14회 전부 갈렸다.** eager 면 0개, lazy 면 4~8개. 중간이 없다.
양성 대조(Wikipedia, 실제로 lazy)도 lazy 쪽에 정확히 붙었다.

[방법론 15조 "시간 전에 횟수를 센다"](../../README.md#방법론-21조) 가 또 맞았다 —
시간으로는 60회를 돌려도 안 갈리는 게 횟수로는 14회에 갈렸다.

### Layout 절감은 팔과 무관했다 (T4)

| 사이트 | `asis` | `forced` |
|---|---:|---:|
| Paris | −41% | −41% |
| London | −50% | −52% |
| Rome | −41% | −29% |

### 판정

| # | 주장 | 판정 | |
|---|---|---|---|
| T1 | `asis` 에서 cv 가 LCP 를 못 줄인다 | **판정 불가** | 검정력 부족 (필요 96회, 실제 5회) |
| T2 | `forced` 에서 cv 가 LCP 를 줄인다 | **판정 불가** | 같은 이유 |
| T3 | 요청 앞당김이 `asis`≈0, `forced` 양수 | **성립 (순서로)** | 시간으로는 32 vs 56ms 로 구분 불가, 순서로는 **14/14** |
| T4 | Layout 절감은 두 팔 모두 같다 | **성립** | 41~50% vs 29~52% |

### 그래서 조건이 두 개로 좁혀진다

기전은 실제 사이트에서도 확인됐다. 그런데 **기전이 있다고 이득이 생기는 건 아니다.**

> `content-visibility` 가 이미지 LCP 를 앞당기려면 **둘 다** 필요하다:
> ① LCP 이미지가 `loading="lazy"` 일 것 — 아니면 요청이 레이아웃을 안 기다린다
> ② 레이아웃 비용이 **수백 ms** 일 것 — 아니면 앞당길 폭이 잡음보다 작다
>
> C8 의 Wikipedia 3곳은 둘 다 만족했다(lazy · 478~643ms). Wikivoyage 3곳은 **①도 ②도 아니다.**

이건 [C7 의 결론](../c7-real-initial-load/RESULTS.md)과 같은 모양이다 — **렌더링 일이 줄어도 사용자에게 보이려면 그 일이 병목이어야 한다.**

---

## 7. LCP 요소는 바뀌지 않았다

| 사이트 | 모드 | 이미지 / 텍스트 / 못받음 | LCP 크기 |
|---|---|---|---:|
| Milky Way | plain·cv-auto | 6 / 0 / 0 (전 조건) | 52,080px² |
| Grand Canyon | plain·cv-auto | 6 / 0 / 0 | 53,280px² |
| Mount Everest | plain·cv-auto | 6 / 0 / 0 | 55,296px² |

**S4 성립.** 72회 전부 `<img>` 였고 크기도 같았다.
`content-visibility` 가 LCP 요소 자체를 바꾸지는 않는다 — LCP 이미지가 화면 위쪽에 있으면.

---

## 8. 사전 등록 주장 판정

| # | 주장 | 판정 | 실측 |
|---|---|---|---|
| S1 | 초기 Layout 이 크게 준다 | **성립** | 52~90% 절감 (6조건 전부 ≥50%) |
| S2 | LCP 는 안 빨라진다 | **반증** | 0~38% 빨라졌다 |
| S3 | FCP 가 빨라지고 LCP 와 갈라진다 | **성립** | FCP 4~38% 단축, cold 에서 분리 |
| S4 | LCP 요소는 `<img>` 로 유지 | **성립** | 72/72 |
| S5 | cold 에서 FCP/LCP 분리 | **성립** | 117~331ms |
| S6 | cold 에서 LCP 안 빨라진다 | **반증** | 12~28% 빨라졌다 |
| S7 | warm 에서 FCP = LCP | **부분** | 6조건 중 4 |
| S8 | eager 에서 cv 의 LCP 개선이 사라진다 | **부분** | LCP 로는 2/3, 요청 시작으로는 3/3 |
| S9 | eager 가 plain 의 LCP 를 크게 앞당긴다 | **부분** | 요청은 300~400ms, LCP 는 2~23% |
| S10 | eager 에서도 Layout 절감은 그대로 | **성립** | 59~66% vs 61~72% |
| T1 | `asis`(실제 eager) 에서 cv 가 LCP 를 못 줄인다 | **판정 불가** | 검정력 부족 — 필요 96회, 실제 5회 |
| T2 | `forced`(히어로 lazy) 에서 cv 가 LCP 를 줄인다 | **판정 불가** | 같은 이유 |
| T3 | 요청 앞당김이 `asis`≈0, `forced` 양수 | **성립** | 순서로 보면 **14/14** (시간으로는 구분 불가) |
| T4 | Layout 절감은 두 팔 모두 같다 | **성립** | 41~50% vs 29~52% |

**2 반증 · 7 성립 · 3 부분 · 2 판정 불가.**
[C6 에서는 cv 에 불리한 쪽으로, C7 에서는 유리한 쪽으로 편향돼 있었다](../c7-real-initial-load/RESULTS.md).
이번엔 다시 불리한 쪽(S2·S6)으로 예측했고, **그 둘이 정확히 틀렸다.**

---

## 9. 흩어짐

`cold` 는 네트워크가 통째로 들어오므로 흩어짐이 크다. 반복별 LCP (ms, 정렬):

```
Milky Way      cold  plain     1923  2005  2175  2349  3961  5734   3.0배
Milky Way      cold  cv-auto   1589  1673  1909  1935  1991  2589   1.6배
Grand Canyon   cold  plain     1665  1732  2101  2224  2273  2710   1.6배
Grand Canyon   cold  cv-auto   1376  1482  1535  1597  1715  2223   1.6배
Mount Everest  cold  plain     1877  1951  2127  2346  2599  4456   2.4배
Mount Everest  cold  cv-auto   1818  1885  1985  2064  2667  3708   2.0배
```

`plain` 쪽 꼬리(5734ms, 4456ms)가 더 길다. 중앙값으로 읽어야 한다.
`warm` 은 표본이 작은데도 cv-auto 에 6.5배짜리 이상치가 하나 있었다(284~1847ms) — warm 의 −38% 는 그만큼 덜 믿을 만하다.

---

## 10. 남은 것

- **위키 밖.** 조건 넷을 만족하는 공개 페이지를 위키 밖에서 못 찾았다 — 긴 문서는 텍스트 LCP 고,
  이미지 LCP 는 문서가 짧고, 나머지는 봇 차단이었다(문서 744px · DOM 33). 로그인·스크립트가 있는
  실제 커머스·SNS 는 아예 못 건드렸다
- **T1·T2 를 제대로 잡으려면** 셀당 96회가 필요하다. 사이트 3곳 × 4셀 × 96회 = 1,152회 로드
- **레이아웃이 비싼데 LCP 이미지가 eager 인 페이지.** 그게 있어야 ①과 ②를 분리해서 볼 수 있다
- **cv-auto 의 cold Layout 절감(52~65%)이 warm(82~90%)보다 작은 이유.** 이미지 도착마다 리레이아웃이 도는 것으로 보이는데 계수하지 않았다
- **다른 브라우저.** Safari 는 `content-visibility` 를 17.4 에서 넣었고 lazy 정책도 다르다
